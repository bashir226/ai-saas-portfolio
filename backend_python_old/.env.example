# FlowForge API
PORT=8000
HOST=127.0.0.1
DATABASE_URL=./flowforge.db
JWT_SECRET=replace-with-a-long-random-secret

# Optional. Without it, workflows use the deterministic demo provider.
OPENAI_API_KEY=
